export default function PublicLoading() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-[var(--bg-deep)]">
      <div className="animate-pulse text-[var(--neon-cyan)] text-xl font-display">Loading...</div>
    </div>
  );
}
